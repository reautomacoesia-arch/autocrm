'use client'
import type { Lead } from '@/lib/types'
interface Props { lead: Lead; onClose: () => void; onConverted: () => void }
export default function ConvertToClientModal(_props: Props) {
  return <div />
}
